<li>
  <span><%= question.body %></span><br />
  <%= link_to t('watch'), admin_question_path(question) %> |
  <%= link_to t('edit'), edit_admin_question_path(question) %> |
  <%= link_to t('delete'), admin_question_path(question), method: :delete, data: { confirm: t('delete_confirm') } %>
</li>
