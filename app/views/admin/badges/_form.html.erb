<h1>Форма для награды</h1>
<hr />
<%= form_with model: [:admin, @badge] do |f| %>
  <%= render 'shared/errors', resource: @badge %>

  <p><%= f.label :title %></p>
  <p><%= f.text_field :title %></p>

  <p><%= f.label :img_name %></p>
  <p><%= f.select :img_name, @images %></p>

  <p><%= f.label :rule %></p>
  <p><%= f.select :rule, @rules %></p>

  <p><%= f.label :description %></p>
  <p><%= f.text_area :description, size: "50x5" %></p>

  <%= f.submit class: "btn btn-primary" %>
  <%= link_to "Назад", badges_path, class: "btn btn-primary" %>
<% end %>
